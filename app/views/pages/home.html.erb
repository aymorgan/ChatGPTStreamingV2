<div>
  <h1 class="font-bold text-4xl">Homepage</h1>
  <p>Find me in app/views/pages/home.html.erb</p>

    <% if current_user %>
        <p>Logged in as <%= current_user.email %></p>
        
        <%= link_to 'Start a new chat', chats_path, data: { turbo_method: :post }, class: "text-green-600 underline" %>
      <% end %>

  
</div>
